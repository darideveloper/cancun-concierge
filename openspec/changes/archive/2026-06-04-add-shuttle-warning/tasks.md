## 1. Styling Implementation

- [x] 1.1 Update `css/_rutva-deb-airport-transfers.sass` to include `.shuttle-warning` styles under `main.rutva-deb .transportation .titles`.
- [x] 1.3 Reduce `h1.small` font size by 20% in `css/_rutva-deb-airport-transfers.sass`.
- [x] 1.2 (Optional) If SASS compilation is required, ensure `css/styles.css` is updated.

## 2. HTML Implementation

- [x] 2.1 Update `rutva-deb-airport-transfers/index.html`: Add `<p class="shuttle-warning">` with the shuttle service warning message under the `h1.small`.

## 3. Verification

- [x] 3.1 Verify the warning message appears correctly on the Rutva & Deb event page.
- [x] 3.2 Ensure the styling is responsive and legible across different screen sizes.
- [x] 3.3 Verify H1 font size is reduced by 20%.
