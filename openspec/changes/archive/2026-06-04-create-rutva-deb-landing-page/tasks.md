## 1. File Scaffolding

- [x] 1.1 Create the dedicated directory `rutva-deb-airport-transfers/`
- [x] 1.2 Copy the HTML template from `kiana-swaroop-airport-transfers/index.html` to the new directory
- [x] 1.3 Copy the template JavaScript from `code/kiana-swaroop-airport-transfers.js` to `code/rutva-deb-airport-transfers.js`
- [x] 1.4 Copy the template Sass partial from `css/_kiana-swaroop-airport-transfers.sass` to `css/_rutva-deb-airport-transfers.sass`

## 2. Content & Logic Customization

- [x] 2.1 Update `rutva-deb-airport-transfers/index.html` branding and paths (Replace "kiana-swaroop" with "rutva-deb")
- [x] 2.2 Update `code/rutva-deb-airport-transfers.js` with the correct `stripe_user` and event labels
- [x] 2.3 Update `css/_rutva-deb-airport-transfers.sass` selectors and background image paths
- [x] 2.4 Ensure "Hilton Tulum Riviera Maya" is pre-selected in the HTML form dropdowns

## 3. Global Integration

- [x] 3.1 Import the new Sass partial in `css/styles.sass`
- [x] 3.2 Create the placeholder asset `imgs/rutva-deb-logo.webp`

## 4. Verification

- [x] 4.1 Verify that navigating to `/rutva-deb-airport-transfers/` displays "Rutva & Deb" branding
- [x] 4.2 Confirm that form submission uses the `rutva-deb` Stripe identifier (inspect network request)
- [x] 4.3 Confirm that all responsive views (mobile/tablet/desktop) render correctly
