## 1. Implement Changes

- [x] 1.1 Remove the "Social responsability" and "Los cabos" list item (`<li>`) links from the navigation bar in `/develop/vanilla/cancun-concierge/rutva-dev-airport-transfers/index.html`.
- [x] 1.2 Add the mobile menu height override in `/develop/vanilla/cancun-concierge/css/_rutva-dev-airport-transfers.sass` to set the expanded nav height to `240px` under `max-width: 790px`.
- [x] 1.3 Compile the CSS stylesheets from SASS by running `npx sass css/styles.sass css/styles.css`.

## 2. Verification

- [x] 2.1 Verify that the remaining header links (Home, Services, Transportation) still render and navigate correctly on desktop.
- [x] 2.2 Verify that the "Social responsability" and "Los cabos" links are completely removed from the header.
- [x] 2.3 Verify that on mobile screens (< 790px), the expanded mobile navigation menu height is exactly `240px` to fit the 3 links without extra empty space at the bottom.
