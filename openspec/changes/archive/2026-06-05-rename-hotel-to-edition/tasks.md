## 1. Preparation

- [x] 1.1 Search for all occurrences of "Hilton Tulum Riviera Maya" within the `rutva-deb-airport-transfers/` directory.
- [x] 1.2 Check `code/rutva-deb-airport-transfers.js` for any logic that depends on the "Hilton Tulum Riviera Maya" string.

## 2. Implementation

- [x] 2.1 Replace "Hilton Tulum Riviera Maya" with "Hotel Edition" in the H1 tag of `rutva-deb-airport-transfers/index.html`.
- [x] 2.2 Replace "Hilton Tulum Riviera Maya" with "Hotel Edition" in the `<option>` tags of both `arriving-hotel` and `departing-hotel` select elements.
- [x] 2.3 Replace all instances of "Hilton Tulum Riviera Maya" in the FAQs and body text of `rutva-deb-airport-transfers/index.html`.

## 3. Verification and Testing

- [x] 3.1 Verify that NO instances of "Hilton Tulum Riviera Maya" remain in the Rutva & Deb page.
- [x] 3.2 Confirm that "Cancun Concierge" branding is still intact.
- [x] 3.3 Ensure the form still submits correctly and pricing (if handled in JS) is not affected.
