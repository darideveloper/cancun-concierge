## 1. HTML Markup

- [x] 1.1 Add the paragraph element with the names "Dana & Hirshal" inside the `.paralax` div in `/dana-hirshal-airport-transfers/index.html`.

## 2. Style Implementation

- [x] 2.1 Update the base `.paralax` styling in `/css/_dana-hirshal-airport-transfers.sass` to center, size, and shade the parallax paragraph text.
- [x] 2.2 Add tablet responsive text scaling under the `max-width: 900px` media query in `/css/_dana-hirshal-airport-transfers.sass`.
- [x] 2.3 Add mobile responsive text scaling under the `max-width: 500px` media query in `/css/_dana-hirshal-airport-transfers.sass`.

## 3. Style Compilation

- [x] 3.1 Compile the SASS rules to CSS by running `npx sass css/styles.sass css/styles.css`.
