## Why

The rutva-deb-airport-transfers booking page displays price labels using the shorthand "/ person" (e.g., "$52.00 USD / person"), which looks informal and inconsistent with standard pricing presentation. Replacing it with "per person" reads more naturally and professionally.

## What Changes

- Replace the string `"/ person"` with `" per person"` in `code/rutva-deb-airport-transfers.js`

## Capabilities

### New Capabilities

None — no new capabilities introduced.

### Modified Capabilities

None — no spec-level behavior changes.

## Impact

- **File modified**: `code/rutva-deb-airport-transfers.js` (single line change)
- No impact on pricing logic, Stripe integration, UI structure, or any other page
