## MODIFIED Requirements

### Requirement: Price label format
The system SHALL display shared shuttle prices with the text "per person" instead of "/ person".

#### Scenario: Price label displays correctly
- **WHEN** a shared shuttle price is shown on the booking page
- **THEN** the price label SHALL use "per person" (e.g., "$52.00 USD per person")
