## 1. File Renaming & Git Moves

- [x] 1.1 Rename the landing page directory from `rutva-deb-airport-transfers` to `rutva-dev-airport-transfers` using `git mv`
- [x] 1.2 Rename the SASS partial stylesheet from `css/_rutva-deb-airport-transfers.sass` to `css/_rutva-dev-airport-transfers.sass` using `git mv`
- [x] 1.3 Rename the JavaScript logic file from `code/rutva-deb-airport-transfers.js` to `code/rutva-dev-airport-transfers.js` using `git mv`
- [x] 1.4 Rename the logo image from `imgs/rutva-deb-logo.webp` to `imgs/rutva-dev-logo.webp` using `git mv`

## 2. Style updates

- [x] 2.1 Update the import in `css/styles.sass` from `rutva-deb-airport-transfers` to `rutva-dev-airport-transfers`
- [x] 2.2 Update class selectors and logo background image path in `css/_rutva-dev-airport-transfers.sass`
- [x] 2.3 Rebuild the stylesheet by compiling SASS to `css/styles.css`

## 3. HTML Content and Warning Implementation

- [x] 3.1 Update class names (`rutva-deb` $\rightarrow$ `rutva-dev`), script source, and text instances of "Deb" to "Dev" in `rutva-dev-airport-transfers/index.html`
- [x] 3.2 Add the new Uber warning element in the hero titles block in `rutva-dev-airport-transfers/index.html`

## 4. Documentation updates

- [x] 4.1 Update all references of "Deb" to "Dev" and the page route in `openspec/specs/rutva-deb-landing-page/spec.md`

